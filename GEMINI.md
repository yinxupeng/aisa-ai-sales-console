# 企微智能销售原型工程规范与开发准则（科学实践版）

本工程是一套基于 React 18 + Ant Design 5 的高保真交互静态原型系统，采用 esbuild 进行快速构建。为了兼顾**长期代码健康度**与**敏捷原型的开发速度**，全流程开发必须遵循以下多维度科学准则：

---

## 1. 适度内聚与规模控制（Balanced Modularity）
- **顶层装配极简**：`src/App.jsx` 仅作为系统顶层布局（AppShell）、登录认证（Login）与顶层路由映射（content），代码规模严格控制在 250~400 行左右。
- **业务页面独立化**：每个独立业务场景统一落入 `src/pages/<PageName>.jsx`，单文件体积保持在 300~800 行的最佳认知区间。
- **避免碎片化与三次复用原则（Rule of Three）**：
  - 仅在当前页面内部使用的小弹窗、小卡片，**允许就近声明在当前页面文件内**，避免过度碎片化导致“找代码困难”。
  - 只有当组件被 **2 个及以上独立页面复用**时（如标签多选器、知识库选择弹窗、企微头像），才沉淀至 `src/components/`。

---

## 2. 分级数据收敛（Tiered Data Decoupling）
- **全局真理源统一（Global Entity Data）**：涉及跨页面联动的核心业务数据（客户列表、会话流、智能体配置、标签库、企微账号），必须统一维护在 `src/data/`（如 `appData.js`, `conversations.js`），确保全系统数据联动一致，为后续对接后端 API 提供标准数据层。
- **局部瞬态枚举就近（Local Ephemeral Config）**：仅属于单一表单、单个筛选器的简单选项数组（如状态切换 `["全部", "开启", "关闭"]`），允许就近声明在当前组件顶部，避免形式主义的跨文件抽离。

---

## 3. 依赖闭环与干净演进（Safe Refactoring）
- **物理删除前做静态引用扫描**：当新方案替代旧版本时，必须通过全局搜索确认废弃代码（如旧组件、已废弃字段）在全工程的引用数为 0，方可物理删除，杜绝误伤暗雷。
- **杜绝注释残留**：不在代码库保留大段注释掉的代码或失活的占位弹窗，Git 提交历史即为完备的回溯安全网。

---

## 4. 韧性自动化验证（Resilient Verification）
每次代码变更交付前，必须执行闭环自测，坚守质量底线：
1. **静态编译构建（硬性底线）**：运行 `node scripts/build.mjs`，确保 esbuild 打包编译 0 报错、0 警告。
2. **运行时挂载与依赖健全（零白屏）**：
   - 提取或重构文件时，必须核实所有引用的外部组件（如 Antd 子组件 `Paragraph/Title/Text`、通用公共组件、数据变量）均已显式 `import`。
   - 禁止让组件出现隐蔽的 `ReferenceError` 或在 `file:///` 协议下触发未捕获的脱敏 `Script error.`。
3. **去除文案脆弱性**：自动化测试聚焦在“依赖是否完备、页面能否挂载、数据能否流转”，避免硬编码死卡具体的展示文案，确保业务原型文案调整的灵活性。
